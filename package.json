{
  "name": "@arianee/ts-contracts",
  "version": "1.0.0",
  "description": "typescript typings for arianee contracts",
  "main": "",
  "scripts": {
    "publish":"npm run build && git add . && git commit --amend --no-edit && git push",
    "build": "./node_modules/.bin/typechain --target=web3-1.0.0 './src/abi/*.json' --outDir=./src"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "typechain": "^0.3.21"
  }
}